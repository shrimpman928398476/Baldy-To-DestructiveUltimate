
if getgenv().offguard then
local SkillName = "Off-Guard" --[ skills cant have the same name or it will bug, ]
local CooldownLengh = 5
local Burning = false -- [ the KJ UFW burning effect ]

local Activated = function()
local animation = Instance.new("Animation")
animation.AnimationId = "rbxassetid://136370737633649"
        
        local chr = game.Players.LocalPlayer.Character
        local humanoid = chr:WaitForChild("Humanoid")
        
        local animTrack = humanoid:LoadAnimation(animation)
        animTrack:Play()
animTrack:AdjustSpeed(1.35)
animTrack.TimePosition = 1.75
        task.delay(1, 

function() 

animTrack:Stop()


end)
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

local function getClosestPlayer()
    local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
    local root = character:WaitForChild("HumanoidRootPart")

    local closestDistance = math.huge
    local closestPlayer = nil

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= localPlayer and player.Character then
            local otherRoot = player.Character:FindFirstChild("HumanoidRootPart")
            if otherRoot then
                local distance = (root.Position - otherRoot.Position).Magnitude
                if distance < closestDistance then
                    closestDistance = distance
                    closestPlayer = player
                end
            end
        end
    end

    return closestPlayer
end

task.wait(1) -- small delay so characters load

local target = getClosestPlayer()
if target and target.Character then
    local targetRoot = target.Character:FindFirstChild("HumanoidRootPart")
    if targetRoot then
        localPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = targetRoot.CFrame
        print("Teleported to closest player:", target.Name)
    end
end
    local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local hrp = character:WaitForChild("HumanoidRootPart")

-- Function: find closest player
local function getClosestPlayer()
    local closest = nil
    local shortest = math.huge
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local dist = (plr.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
            if dist < shortest then
                closest = plr
                shortest = dist
            end
        end
    end

    return closest
end

-- Begin action -------------------------------------------------------

local target = getClosestPlayer()
if not target then return end

local targetHRP = target.Character.HumanoidRootPart

-- Store original states
local originalCFrame = hrp.CFrame
local originalAutoRotate = humanoid.AutoRotate

-- Disable rotation systems
humanoid.AutoRotate = false
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0

-- Face down (180 degrees on X)
local faceDown = CFrame.Angles(math.rad(-90), 0, 0)

-- KEEP POSITION LOCKED FOR 1 SECOND
local connection
connection = RunService.RenderStepped:Connect(function()
    -- 5 studs above closest player
    local lockPos = targetHRP.Position + Vector3.new(0, 5.5, 0)
    
    -- Apply the rotation + locked position
    hrp.CFrame = CFrame.new(lockPos) * faceDown
    
    -- Zero angular rotation
    hrp.AssemblyAngularVelocity = Vector3.zero
end)
game:GetService("Players").LocalPlayer.Character.Communicate:FireServer(unpack({
    [1] = {
        ["Tool"] = game:GetService("Players").LocalPlayer.Backpack["Normal Punch"],
        ["Goal"] = "Console Move"
    }
}))
task.wait(1.2)

-- Restore normal state
connection:Disconnect()
humanoid.AutoRotate = originalAutoRotate
humanoid.WalkSpeed = 16
humanoid.JumpPower = 50
hrp.CFrame = originalCFrame

end

-- [[ THE CODE, MODIFY IF YOU WANT. ]]
local SkillTemplate = Instance.new("Tool")
SkillTemplate.Name = SkillName
SkillTemplate.RequiresHandle = false
SkillTemplate.ManualActivationOnly = false
SkillTemplate.CanBeDropped = false
SkillTemplate:SetAttribute("Regged", false)
SkillTemplate:SetAttribute("Skill", true)
SkillTemplate.Parent = game.Players.LocalPlayer.Backpack
local ks = "5"
local OnCooldown = false
local cooldownFunction = function()
OnCooldown = true
local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	ks = "" .. slot.Parent.Parent.Name
cd.Parent = slot.Parent
local cdTween = game:GetService("TweenService"):Create(cd, TweenInfo.new(CooldownLengh), {Size = UDim2.new(1, 0, -0.12, 0)})
cdTween:Play()
cdTween.Completed:Connect(function()
OnCooldown = false
cd:Destroy()
slot.Parent.BackgroundColor3 = Color3.fromRGB(26, 125, 255)
game:GetService("TweenService"):Create(slot.Parent, TweenInfo.new(1), {BackgroundColor3 = Color3.fromRGB(31, 31, 31)}):Play()
end)
    end
end
end

local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	if Burning then
	local fireLiterally = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Flipbook:Clone()
fireLiterally.Parent = slot.Parent
fireLiterally.LocalScript.Enabled = true
end
	slot.Parent.MouseButton1Click:Connect(function()
	if not OnCooldown then
cooldownFunction()
Activated()
end
	end)
	end
end

local Kmg = {
    ["1"] = Enum.KeyCode.One,
    ["2"] = Enum.KeyCode.Two,
    ["3"] = Enum.KeyCode.Three,
    ["4"] = Enum.KeyCode.Four,
    ["5"] = Enum.KeyCode.Five,
    ["6"] = Enum.KeyCode.Six,
    ["7"] = Enum.KeyCode.Seven,
    ["8"] = Enum.KeyCode.Eight,
    ["9"] = Enum.KeyCode.Nine,
    ["0"] = Enum.KeyCode.Zero
}
local kb = Kmg[ks]

local uis = game:GetService("UserInputService") local con = uis.InputBegan:Connect(function(input) if input.KeyCode == kb then
if not OnCooldown and game:GetService("Players").LocalPlayer.PlayerGui.Chat.Frame.ChatBarParentFrame.Frame.BoxFrame.Frame.TextLabel.Visible then
cooldownFunction()
Activated()
end
 end end) game.Players.LocalPlayer.Character:WaitForChild("Humanoid").Died:Connect(function() con:Disconnect() end)

game.Players.LocalPlayer.Character.Humanoid.Died:Connect(function()
died = true
end) end

if getgenv().skysender then

local SkillName = "Sky Sender" --[ skills cant have the same name or it will bug, ]
local CooldownLengh = 5
local Burning = false -- [ the KJ UFW burning effect ]

local Activated = function()

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

local function getClosestPlayer()
    local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
    local root = character:WaitForChild("HumanoidRootPart")

    local closestDistance = math.huge
    local closestPlayer = nil

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= localPlayer and player.Character then
            local otherRoot = player.Character:FindFirstChild("HumanoidRootPart")
            if otherRoot then
                local distance = (root.Position - otherRoot.Position).Magnitude
                if distance < closestDistance then
                    closestDistance = distance
                    closestPlayer = player
                end
            end
        end
    end

    return closestPlayer
end

task.wait(0.1) -- small delay so characters load

local target = getClosestPlayer()
if target and target.Character then
    local targetRoot = target.Character:FindFirstChild("HumanoidRootPart")
    if targetRoot then
        localPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = targetRoot.CFrame
        print("Teleported to closest player:", target.Name)
    end
end
    local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local hrp = character:WaitForChild("HumanoidRootPart")

-- Function: find closest player
local function getClosestPlayer()
    local closest = nil
    local shortest = math.huge
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local dist = (plr.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
            if dist < shortest then
                closest = plr
                shortest = dist
            end
        end
    end

    return closest
end

-- Begin action -------------------------------------------------------

local target = getClosestPlayer()
if not target then return end

local targetHRP = target.Character.HumanoidRootPart

-- Store original states
local originalCFrame = hrp.CFrame
local originalAutoRotate = humanoid.AutoRotate

-- Disable rotation systems
humanoid.AutoRotate = false
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0

-- Face down (180 degrees on X)
local faceDown = CFrame.Angles(math.rad(90), 0, 0)

-- KEEP POSITION LOCKED FOR 1 SECOND
local connection
connection = RunService.RenderStepped:Connect(function()
    -- 5 studs above closest player
    local lockPos = targetHRP.Position + Vector3.new(0, -4.5, 0)
    
    -- Apply the rotation + locked position
    hrp.CFrame = CFrame.new(lockPos) * faceDown
    
    -- Zero angular rotation
    hrp.AssemblyAngularVelocity = Vector3.zero
end)
game:GetService("Players").LocalPlayer.Character.Communicate:FireServer(unpack({
    [1] = {
        ["Tool"] = game:GetService("Players").LocalPlayer.Backpack["Normal Punch"],
        ["Goal"] = "Console Move"
    }
}))
task.wait(1.2)

-- Restore normal state
connection:Disconnect()
humanoid.AutoRotate = originalAutoRotate
humanoid.WalkSpeed = 16
humanoid.JumpPower = 50
hrp.CFrame = originalCFrame


end

-- [[ THE CODE, MODIFY IF YOU WANT. ]]
local SkillTemplate = Instance.new("Tool")
SkillTemplate.Name = SkillName
SkillTemplate.RequiresHandle = false
SkillTemplate.ManualActivationOnly = false
SkillTemplate.CanBeDropped = false
SkillTemplate:SetAttribute("Regged", false)
SkillTemplate:SetAttribute("Skill", true)
SkillTemplate.Parent = game.Players.LocalPlayer.Backpack
local ks = "5"
local OnCooldown = false
local cooldownFunction = function()
OnCooldown = true
local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	ks = "" .. slot.Parent.Parent.Name
cd.Parent = slot.Parent
local cdTween = game:GetService("TweenService"):Create(cd, TweenInfo.new(CooldownLengh), {Size = UDim2.new(1, 0, -0.12, 0)})
cdTween:Play()
cdTween.Completed:Connect(function()
OnCooldown = false
cd:Destroy()
slot.Parent.BackgroundColor3 = Color3.fromRGB(26, 125, 255)
game:GetService("TweenService"):Create(slot.Parent, TweenInfo.new(1), {BackgroundColor3 = Color3.fromRGB(31, 31, 31)}):Play()
end)
    end
end
end

local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	if Burning then
	local fireLiterally = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Flipbook:Clone()
fireLiterally.Parent = slot.Parent
fireLiterally.LocalScript.Enabled = true
end
	slot.Parent.MouseButton1Click:Connect(function()
	if not OnCooldown then
cooldownFunction()
Activated()
end
	end)
	end
end

local Kmg = {
    ["1"] = Enum.KeyCode.One,
    ["2"] = Enum.KeyCode.Two,
    ["3"] = Enum.KeyCode.Three,
    ["4"] = Enum.KeyCode.Four,
    ["5"] = Enum.KeyCode.Five,
    ["6"] = Enum.KeyCode.Six,
    ["7"] = Enum.KeyCode.Seven,
    ["8"] = Enum.KeyCode.Eight,
    ["9"] = Enum.KeyCode.Nine,
    ["0"] = Enum.KeyCode.Zero
}
local kb = Kmg[ks]

local uis = game:GetService("UserInputService") local con = uis.InputBegan:Connect(function(input) if input.KeyCode == kb then
if not OnCooldown and game:GetService("Players").LocalPlayer.PlayerGui.Chat.Frame.ChatBarParentFrame.Frame.BoxFrame.Frame.TextLabel.Visible then
cooldownFunction()
Activated()
end
 end end) game.Players.LocalPlayer.Character:WaitForChild("Humanoid").Died:Connect(function() con:Disconnect() end)

game.Players.LocalPlayer.Character.Humanoid.Died:Connect(function()
died = true
end)  end

if getgenv().blitzstrike then
local SkillName = "Blitz Strike" --[ skills cant have the same name or it will bug, ]
local CooldownLengh = 5
local Burning = false -- [ the KJ UFW burning effect ]

local Activated = function()

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

local function getClosestPlayer()
    local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
    local root = character:WaitForChild("HumanoidRootPart")

    local closestDistance = math.huge
    local closestPlayer = nil

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= localPlayer and player.Character then
            local otherRoot = player.Character:FindFirstChild("HumanoidRootPart")
            if otherRoot then
                local distance = (root.Position - otherRoot.Position).Magnitude
                if distance < closestDistance then
                    closestDistance = distance
                    closestPlayer = player
                end
            end
        end
    end

    return closestPlayer
end

task.wait(0.1) -- small delay so characters load

local target = getClosestPlayer()
if target and target.Character then
    local targetRoot = target.Character:FindFirstChild("HumanoidRootPart")
    if targetRoot then
        localPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = targetRoot.CFrame
        print("Teleported to closest player:", target.Name)
    end
end
    local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local hrp = character:WaitForChild("HumanoidRootPart")

-- Function: find closest player
local function getClosestPlayer()
    local closest = nil
    local shortest = math.huge
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local dist = (plr.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
            if dist < shortest then
                closest = plr
                shortest = dist
            end
        end
    end

    return closest
end

-- Begin action -------------------------------------------------------

local target = getClosestPlayer()
if not target then return end

local targetHRP = target.Character.HumanoidRootPart

-- Store original states
local originalCFrame = hrp.CFrame
local originalAutoRotate = humanoid.AutoRotate

-- Disable rotation systems
humanoid.AutoRotate = false
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0

-- Face down (180 degrees on X)
local faceDown = CFrame.Angles(math.rad(-90), 0, 0)

-- KEEP POSITION LOCKED FOR 1 SECOND
local connection
connection = RunService.RenderStepped:Connect(function()
    -- 5 studs above closest player
    local lockPos = targetHRP.Position + Vector3.new(0, 4.75, 0)
    
    -- Apply the rotation + locked position
    hrp.CFrame = CFrame.new(lockPos) * faceDown
    
    -- Zero angular rotation
    hrp.AssemblyAngularVelocity = Vector3.zero
end)
game:GetService("Players").LocalPlayer.Character.Communicate:FireServer(unpack({
    [1] = {
        ["Tool"] = game:GetService("Players").LocalPlayer.Backpack["Shove"],
        ["Goal"] = "Console Move"
    }
}))
task.wait(1.2)

-- Restore normal state
connection:Disconnect()
humanoid.AutoRotate = originalAutoRotate
humanoid.WalkSpeed = 16
humanoid.JumpPower = 50
hrp.CFrame = originalCFrame

end

-- [[ THE CODE, MODIFY IF YOU WANT. ]]
local SkillTemplate = Instance.new("Tool")
SkillTemplate.Name = SkillName
SkillTemplate.RequiresHandle = false
SkillTemplate.ManualActivationOnly = false
SkillTemplate.CanBeDropped = false
SkillTemplate:SetAttribute("Regged", false)
SkillTemplate:SetAttribute("Skill", true)
SkillTemplate.Parent = game.Players.LocalPlayer.Backpack
local ks = "5"
local OnCooldown = false
local cooldownFunction = function()
OnCooldown = true
local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	ks = "" .. slot.Parent.Parent.Name
cd.Parent = slot.Parent
local cdTween = game:GetService("TweenService"):Create(cd, TweenInfo.new(CooldownLengh), {Size = UDim2.new(1, 0, -0.12, 0)})
cdTween:Play()
cdTween.Completed:Connect(function()
OnCooldown = false
cd:Destroy()
slot.Parent.BackgroundColor3 = Color3.fromRGB(26, 125, 255)
game:GetService("TweenService"):Create(slot.Parent, TweenInfo.new(1), {BackgroundColor3 = Color3.fromRGB(31, 31, 31)}):Play()
end)
    end
end
end

local cd = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Cooldown:Clone()
for _, slot in pairs(game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.Hotbar:GetDescendants()) do
    if slot:IsA("TextLabel") and slot.Name == "ToolName" and slot.Text == SkillName then
	if Burning then
	local fireLiterally = game:GetService("Players").LocalPlayer.PlayerGui.Hotbar.Backpack.LocalScript.Flipbook:Clone()
fireLiterally.Parent = slot.Parent
fireLiterally.LocalScript.Enabled = true
end
	slot.Parent.MouseButton1Click:Connect(function()
	if not OnCooldown then
cooldownFunction()
Activated()
end
	end)
	end
end

local Kmg = {
    ["1"] = Enum.KeyCode.One,
    ["2"] = Enum.KeyCode.Two,
    ["3"] = Enum.KeyCode.Three,
    ["4"] = Enum.KeyCode.Four,
    ["5"] = Enum.KeyCode.Five,
    ["6"] = Enum.KeyCode.Six,
    ["7"] = Enum.KeyCode.Seven,
    ["8"] = Enum.KeyCode.Eight,
    ["9"] = Enum.KeyCode.Nine,
    ["0"] = Enum.KeyCode.Zero
}
local kb = Kmg[ks]

local uis = game:GetService("UserInputService") local con = uis.InputBegan:Connect(function(input) if input.KeyCode == kb then
if not OnCooldown and game:GetService("Players").LocalPlayer.PlayerGui.Chat.Frame.ChatBarParentFrame.Frame.BoxFrame.Frame.TextLabel.Visible then
cooldownFunction()
Activated()
end
 end end) game.Players.LocalPlayer.Character:WaitForChild("Humanoid").Died:Connect(function() con:Disconnect() end)

game.Players.LocalPlayer.Character.Humanoid.Died:Connect(function()
died = true
end) end

if getgenv().captions then
--<<Ultimate Animation>>--

humanoid.AnimationPlayed:Connect(onAnimationPlayed)


local animationId = 12447707844


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local function onAnimationPlayed(animationTrack)

    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then

local p = game.Players.LocalPlayer

local Humanoid = p.Character:WaitForChild("Humanoid")


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end


local AnimAnim = Instance.new("Animation")

AnimAnim.AnimationId = "rbxassetid://18897551628"

local Anim = Humanoid:LoadAnimation(AnimAnim)


local startTime = 0


Anim:Play()

Anim:AdjustSpeed(0)

Anim.TimePosition = startTime

Anim:AdjustSpeed(0.375)
local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local s = Instance.new("ScreenGui")
s.Parent = playerGui

local textLabel = Instance.new("TextLabel")
textLabel.Parent = s
textLabel.Position = UDim2.new(0.5, -200, 0.8, -50)
textLabel.Size = UDim2.new(0, 400, 0, 50)
textLabel.TextSize = 22.5
textLabel.TextColor3 = Color3.fromRGB(255, 255, 255) --text color
textLabel.Font = Enum.Font.Antique -- font here
textLabel.BackgroundTransparency = 1
textLabel.Text = "" -- dont change
textLabel.TextStrokeTransparency = 0
textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) --stroke color here

local function type(text)
    local index = 1
    while index <= #text do
        textLabel.Text = string.sub(text, 1, index)
        index = index + 1
        wait(0.02)
    end
end

local function f()
    for i = 1, 20 do
        textLabel.TextTransparency = i / 20
        wait(0.01)
    end
end
local function fs()
coroutine.wrap(function()
textLabel.TextStrokeTransparency = 0.1
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.2
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.3
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.4
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.5
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.6
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.7
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.8
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.9
task.wait(0.01)
textLabel.TextStrokeTransparency = 1
end)()
end
coroutine.wrap(function()
    local m = "Guess Ill Have To Go Full Out" -- text here
    type(m)
    wait(0)
    fs() -- this for stroke lmao
    f()
end)()

    end

end

--<<End of the Ultimate animation>>--

--[[Move 1 ULT]]
humanoid.AnimationPlayed:Connect(onAnimationPlayed)

local animationId = 12983333733


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local function onAnimationPlayed(animationTrack)

    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then


local p = game.Players.LocalPlayer

local Humanoid = p.Character:WaitForChild("Humanoid")


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end


local AnimAnim = Instance.new("Animation")

AnimAnim.AnimationId = "rbxassetid://79761806706382"

local Anim = Humanoid:LoadAnimation(AnimAnim)


local startTime = 0


Anim:Play()

Anim:AdjustSpeed(0)

Anim.TimePosition = startTime

Anim:AdjustSpeed(0.55)
task.wait(5)
local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local s = Instance.new("ScreenGui")
s.Parent = playerGui

local textLabel = Instance.new("TextLabel")
textLabel.Parent = s
textLabel.Position = UDim2.new(0.5, -200, 0.8, -50)
textLabel.Size = UDim2.new(0, 400, 0, 50)
textLabel.TextSize = 22.5
textLabel.TextColor3 = Color3.fromRGB(255, 255, 255) --text color
textLabel.Font = Enum.Font.Antique -- font here
textLabel.BackgroundTransparency = 1
textLabel.Text = "" -- dont change
textLabel.TextStrokeTransparency = 0
textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) --stroke color here

local function type(text)
    local index = 1
    while index <= #text do
        textLabel.Text = string.sub(text, 1, index)
        index = index + 1
        wait(0.02)
    end
end

local function f()
    for i = 1, 20 do
        textLabel.TextTransparency = i / 20
        wait(0.01)
    end
end
local function fs()
coroutine.wrap(function()
textLabel.TextStrokeTransparency = 0.1
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.2
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.3
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.4
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.5
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.6
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.7
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.8
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.9
task.wait(0.01)
textLabel.TextStrokeTransparency = 1
end)()
end
coroutine.wrap(function()
    local m = "THATS NOT MY ORDER" -- text here
    type(m)
    wait(0)
    fs() -- this for stroke lmao
    f()
end)()
    end

end

--[[END OF MOVE 1 ULT ANIM]]
--[[Move 2 ULT]]
humanoid.AnimationPlayed:Connect(onAnimationPlayed)

local animationId = 11365563255


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local function onAnimationPlayed(animationTrack)

    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then


local p = game.Players.LocalPlayer

local Humanoid = p.Character:WaitForChild("Humanoid")


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end


local AnimAnim = Instance.new("Animation")

AnimAnim.AnimationId = "rbxassetid://140164642047188"

local Anim = Humanoid:LoadAnimation(AnimAnim)


local startTime = 0


Anim:Play()

Anim:AdjustSpeed(0)

Anim.TimePosition = startTime

Anim:AdjustSpeed(1.35)

local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local s = Instance.new("ScreenGui")
s.Parent = playerGui

local textLabel = Instance.new("TextLabel")
textLabel.Parent = s
textLabel.Position = UDim2.new(0.5, -200, 0.8, -50)
textLabel.Size = UDim2.new(0, 400, 0, 50)
textLabel.TextSize = 22.5
textLabel.TextColor3 = Color3.fromRGB(255, 255, 255) --text color
textLabel.Font = Enum.Font.Antique -- font here
textLabel.BackgroundTransparency = 1
textLabel.Text = "" -- dont change
textLabel.TextStrokeTransparency = 0
textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) --stroke color here

local function type(text)
    local index = 1
    while index <= #text do
        textLabel.Text = string.sub(text, 1, index)
        index = index + 1
        wait(0.02)
    end
end

local function f()
    for i = 1, 20 do
        textLabel.TextTransparency = i / 20
        wait(0.01)
    end
end
local function fs()
coroutine.wrap(function()
textLabel.TextStrokeTransparency = 0.1
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.2
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.3
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.4
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.5
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.6
task.wait(0.01)
textLabel.TextStrokeTransparency = 0.7
task.wait(0.01)




if getgenv().orbitalstrike then

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local function waitForGui()
local bar = playerGui:FindFirstChild("Bar")
 	if bar then 
local magicHealth = bar:FindFirstChild("MagicHealth")
	if magicHealth then
local textLabel = magicHealth:FindFirstChild("TextLabel")
	if textLabel then
                textLabel.Text = "ORBITAL STRIKE CANNON"
	                playerGui.Bar.MagicHealth.Health.Bar.Bar.ImageColor3 = Color3.fromRGB(255, 128, 26)
	                game:GetService("TweenService"):Create(playerGui.Bar.MagicHealth.Health.Bar.Bar, TweenInfo.new(5, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1, true), {ImageColor3 = Color3.fromRGB(0, 255, 255)}):Play()
                playerGui.Bar.MagicHealth.Health.Bar.Bar.Image = "rbxassetid://17136251960"
                playerGui.Bar.MagicHealth.Health.Glow.Image = "rbxassetid://17136251821"
	                playerGui.Bar.MagicHealth.TextLabel.Font = Enum.Font.Merriweather
	                playerGui.Bar.MagicHealth.TextLabel.TextLabel.Font = Enum.Font.Merriweather
	                playerGui.Bar.MagicHealth.TextLabel.TextSize = 26
	                playerGui.Bar.MagicHealth.TextLabel.TextLabel.TextSize = 26
            end
        end
    end
end

playerGui.DescendantAdded:Connect(findGuiAndSetText)

waitForGui()
--<<Ultimate Animation>>--

humanoid.AnimationPlayed:Connect(onAnimationPlayed)


local animationId = 12447707844


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local function onAnimationPlayed(animationTrack)

    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then

local p = game.Players.LocalPlayer

local Humanoid = p.Character:WaitForChild("Humanoid")


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end


local AnimAnim = Instance.new("Animation")

AnimAnim.AnimationId = "rbxassetid://18897551628"

local Anim = Humanoid:LoadAnimation(AnimAnim)


local startTime = 0


Anim:Play()

Anim:AdjustSpeed(0)

Anim.TimePosition = startTime

Anim:AdjustSpeed(1.35) 
wait(1)
local animation = Instance.new("Animation")
animation.AnimationId = "rbxassetid://136370737633649"
        
        local chr = game.Players.LocalPlayer.Character
        local humanoid = chr:WaitForChild("Humanoid")
        
        local animTrack = humanoid:LoadAnimation(animation)
        animTrack:Play()
animTrack:AdjustSpeed(1.35)
animTrack.TimePosition = 1.75
        task.delay(1, 

function() 

animTrack:Stop()


end)
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

local function getClosestPlayer()
    local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
    local root = character:WaitForChild("HumanoidRootPart")

    local closestDistance = math.huge
    local closestPlayer = nil

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= localPlayer and player.Character then
            local otherRoot = player.Character:FindFirstChild("HumanoidRootPart")
            if otherRoot then
                local distance = (root.Position - otherRoot.Position).Magnitude
                if distance < closestDistance then
                    closestDistance = distance
                    closestPlayer = player
                end
            end
        end
    end

    return closestPlayer
end

task.wait(1) -- small delay so characters load

local target = getClosestPlayer()
if target and target.Character then
    local targetRoot = target.Character:FindFirstChild("HumanoidRootPart")
    if targetRoot then
        localPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = targetRoot.CFrame
        print("Teleported to closest player:", target.Name)
    end
end
    local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local hrp = character:WaitForChild("HumanoidRootPart")

-- Function: find closest player
local function getClosestPlayer()
    local closest = nil
    local shortest = math.huge
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local dist = (plr.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
            if dist < shortest then
                closest = plr
                shortest = dist
            end
        end
    end

    return closest
end

-- Begin action -------------------------------------------------------

local target = getClosestPlayer()
if not target then return end

local targetHRP = target.Character.HumanoidRootPart

-- Store original states
local originalCFrame = hrp.CFrame
local originalAutoRotate = humanoid.AutoRotate

-- Disable rotation systems
humanoid.AutoRotate = false
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0

-- Face down (180 degrees on X)
local faceDown = CFrame.Angles(math.rad(-90), 0, 0)

-- KEEP POSITION LOCKED FOR 1 SECOND
local connection
connection = RunService.RenderStepped:Connect(function()
    -- 5 studs above closest player
    local lockPos = targetHRP.Position + Vector3.new(0, 50, 0)
    
    -- Apply the rotation + locked position
    hrp.CFrame = CFrame.new(lockPos) * faceDown
    
    -- Zero angular rotation
    hrp.AssemblyAngularVelocity = Vector3.zero     local player = game.Players.LocalPlayer
local camera = workspace.CurrentCamera

camera.CameraType = Enum.CameraType.Scriptable

local character = player.Character or player.CharacterAdded:Wait()
local head = character:WaitForChild("Head")

-- HOW CLOSE THE CAMERA IS (you can change this anytime)
local distance = 75 -- smaller = closer, bigger = farther

-- direction the camera sits at relative to the head
local direction = Vector3.new(1, 1.5, -1).Unit -- diagonal from above-behind

game:GetService("RunService").RenderStepped:Connect(function()
    local headPos = head.Position

    -- Calculate camera position based on distance
    local camPos = headPos + direction * distance

    -- Look at the head
    camera.CFrame = CFrame.new(camPos, headPos)
end) 
end)
game:GetService("Players").LocalPlayer.Character.Communicate:FireServer(unpack({
    [1] = {
        ["Tool"] = game:GetService("Players").LocalPlayer.Backpack["Serious Punch"],
        ["Goal"] = "Console Move"
    }
}))      


task.wait(6.5)

Players = game:GetService("Players")
 local speaker = Players.LocalPlayer
 workspace.CurrentCamera:remove()
    task.wait(.1)
     workspace.CurrentCamera.CameraSubject = speaker.Character:FindFirstChildWhichIsA('Humanoid')
     workspace.CurrentCamera.CameraType = "Custom"
     speaker.CameraMinZoomDistance = 0.5
     speaker.CameraMaxZoomDistance = 400
     speaker.CameraMode = "Classic"
     speaker.Character.Head.Anchored = false
task.wait(0.05)
-- Restore normal state
connection:Disconnect()
humanoid.AutoRotate = originalAutoRotate
humanoid.WalkSpeed = 16
humanoid.JumpPower = 50
hrp.CFrame = originalCFrame 
    end

end

--<<End of the Ultimate animation>>--

--[[Move 1 ULT]]
humanoid.AnimationPlayed:Connect(onAnimationPlayed)

local animationId = 12983333733


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local function onAnimationPlayed(animationTrack)

    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then


local p = game.Players.LocalPlayer

local Humanoid = p.Character:WaitForChild("Humanoid")


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end


local AnimAnim = Instance.new("Animation")

AnimAnim.AnimationId = "rbxassetid://79761806706382"

local Anim = Humanoid:LoadAnimation(AnimAnim)


local startTime = 0


Anim:Play()

Anim:AdjustSpeed(0)

Anim.TimePosition = startTime

Anim:AdjustSpeed(0.55)


    end

end

--[[END OF MOVE 1 ULT ANIM]]
--[[Move 1 ULT]]
humanoid.AnimationPlayed:Connect(onAnimationPlayed)

local animationId = 12983333733


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local function onAnimationPlayed(animationTrack)

    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then


local p = game.Players.LocalPlayer

local Humanoid = p.Character:WaitForChild("Humanoid")


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end


local AnimAnim = Instance.new("Animation")

AnimAnim.AnimationId = "rbxassetid://79761806706382"

local Anim = Humanoid:LoadAnimation(AnimAnim)


local startTime = 0


Anim:Play()

Anim:AdjustSpeed(0)

Anim.TimePosition = startTime

Anim:AdjustSpeed(1)


    end

end

--[[END OF MOVE 1 ULT ANIM]]
 end



if getgenv().atomicbomb then

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local function waitForGui()
local bar = playerGui:FindFirstChild("Bar")
 	if bar then 
local magicHealth = bar:FindFirstChild("MagicHealth")
	if magicHealth then
local textLabel = magicHealth:FindFirstChild("TextLabel")
	if textLabel then
                textLabel.Text = "ATOMIC BOMB"
	                playerGui.Bar.MagicHealth.Health.Bar.Bar.ImageColor3 = Color3.fromRGB(255, 35, 0)
	                game:GetService("TweenService"):Create(playerGui.Bar.MagicHealth.Health.Bar.Bar, TweenInfo.new(5, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1, true), {ImageColor3 = Color3.fromRGB(140, 26, 26)}):Play()
                playerGui.Bar.MagicHealth.Health.Bar.Bar.Image = "rbxassetid://17136251960"
                playerGui.Bar.MagicHealth.Health.Glow.Image = "rbxassetid://17136251821"
	                playerGui.Bar.MagicHealth.TextLabel.Font = Enum.Font.Merriweather
	                playerGui.Bar.MagicHealth.TextLabel.TextLabel.Font = Enum.Font.Merriweather
	                playerGui.Bar.MagicHealth.TextLabel.TextSize = 26
	                playerGui.Bar.MagicHealth.TextLabel.TextLabel.TextSize = 26
            end
        end
    end
end

playerGui.DescendantAdded:Connect(findGuiAndSetText)

waitForGui()
--<<Ultimate Animation>>--

humanoid.AnimationPlayed:Connect(onAnimationPlayed)


local animationId = 12447707844


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local function onAnimationPlayed(animationTrack)

    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then

local p = game.Players.LocalPlayer

local Humanoid = p.Character:WaitForChild("Humanoid")


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end


local AnimAnim = Instance.new("Animation")

AnimAnim.AnimationId = "rbxassetid://18897551628"

local Anim = Humanoid:LoadAnimation(AnimAnim)


local startTime = 0


Anim:Play()

Anim:AdjustSpeed(0)

Anim.TimePosition = startTime

Anim:AdjustSpeed(1.35) wait(1)
local animation = Instance.new("Animation")
animation.AnimationId = "rbxassetid://136370737633649"
        
        local chr = game.Players.LocalPlayer.Character
        local humanoid = chr:WaitForChild("Humanoid")
        
        local animTrack = humanoid:LoadAnimation(animation)
        animTrack:Play()
animTrack:AdjustSpeed(1.35)
animTrack.TimePosition = 1.75
        task.delay(1, 

function() 

animTrack:Stop()


end)
 wait(1)
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

local function getClosestPlayer()
    local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
    local root = character:WaitForChild("HumanoidRootPart")

    local closestDistance = math.huge
    local closestPlayer = nil

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= localPlayer and player.Character then
            local otherRoot = player.Character:FindFirstChild("HumanoidRootPart")
            if otherRoot then
                local distance = (root.Position - otherRoot.Position).Magnitude
                if distance < closestDistance then
                    closestDistance = distance
                    closestPlayer = player
                end
            end
        end
    end

    return closestPlayer
end

task.wait(1) -- small delay so characters load

local target = getClosestPlayer()
if target and target.Character then
    local targetRoot = target.Character:FindFirstChild("HumanoidRootPart")
    if targetRoot then
        localPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = targetRoot.CFrame
        print("Teleported to closest player:", target.Name)
    end
end
    local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local hrp = character:WaitForChild("HumanoidRootPart")

-- Function: find closest player
local function getClosestPlayer()
    local closest = nil
    local shortest = math.huge
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local dist = (plr.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
            if dist < shortest then
                closest = plr
                shortest = dist
            end
        end
    end

    return closest
end

-- Begin action -------------------------------------------------------

local target = getClosestPlayer()
if not target then return end

local targetHRP = target.Character.HumanoidRootPart

-- Store original states
local originalCFrame = hrp.CFrame
local originalAutoRotate = humanoid.AutoRotate

-- Disable rotation systems
humanoid.AutoRotate = false
humanoid.WalkSpeed = 0
humanoid.JumpPower = 0

-- Face down (180 degrees on X)
local faceDown = CFrame.Angles(math.rad(-90), 0, 0)

-- KEEP POSITION LOCKED FOR 1 SECOND
local connection
connection = RunService.RenderStepped:Connect(function()
    -- 5 studs above closest player
    local lockPos = targetHRP.Position + Vector3.new(0, 75, 0)
    
    -- Apply the rotation + locked position
    hrp.CFrame = CFrame.new(lockPos) * faceDown
    
    -- Zero angular rotation
    hrp.AssemblyAngularVelocity = Vector3.zero
end)
game:GetService("Players").LocalPlayer.Character.Communicate:FireServer(unpack({
    [1] = {
        ["Tool"] = game:GetService("Players").LocalPlayer.Backpack["Table Flip"],
        ["Goal"] = "Console Move"
    }
}))      

Players = game:GetService("Players")
 local speaker = Players.LocalPlayer
 workspace.CurrentCamera:remove()
     wait(.1)
     workspace.CurrentCamera.CameraSubject = speaker.Character:FindFirstChildWhichIsA('Humanoid')
     workspace.CurrentCamera.CameraType = "Custom"
     speaker.CameraMinZoomDistance = 0.5
     speaker.CameraMaxZoomDistance = 400
     speaker.CameraMode = "Classic"
     speaker.Character.Head.Anchored = false
task.wait(6.5)
-- Restore normal state
connection:Disconnect()
humanoid.AutoRotate = originalAutoRotate
humanoid.WalkSpeed = 16
humanoid.JumpPower = 50
hrp.CFrame = originalCFrame
    end

end

--<<End of the Ultimate animation>>--

--[[Move 1 ULT]]
humanoid.AnimationPlayed:Connect(onAnimationPlayed)

local animationId = 11365563255


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local function onAnimationPlayed(animationTrack)

    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then


local p = game.Players.LocalPlayer

local Humanoid = p.Character:WaitForChild("Humanoid")


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end


local AnimAnim = Instance.new("Animation")

AnimAnim.AnimationId = "rbxassetid://12510170988"

local Anim = Humanoid:LoadAnimation(AnimAnim)


local startTime = 0


Anim:Play()

Anim:AdjustSpeed(0)

Anim.TimePosition = startTime

Anim:AdjustSpeed(1)


    end

end

--[[END OF MOVE 1 ULT ANIM]]
--[[Move 1 ULT]]
humanoid.AnimationPlayed:Connect(onAnimationPlayed)

local animationId = 12983333733


local player = game.Players.LocalPlayer

local character = player.Character or player.CharacterAdded:Wait()

local humanoid = character:WaitForChild("Humanoid")


local function onAnimationPlayed(animationTrack)

    if animationTrack.Animation.AnimationId == "rbxassetid://" .. animationId then


local p = game.Players.LocalPlayer

local Humanoid = p.Character:WaitForChild("Humanoid")


for _, animTrack in pairs(Humanoid:GetPlayingAnimationTracks()) do

    animTrack:Stop()

end


local AnimAnim = Instance.new("Animation")

AnimAnim.AnimationId = "rbxassetid://79761806706382"

local Anim = Humanoid:LoadAnimation(AnimAnim)


local startTime = 0


Anim:Play()

Anim:AdjustSpeed(0)

Anim.TimePosition = startTime

Anim:AdjustSpeed(1)


    end

end

--[[END OF MOVE 1 ULT ANIM]]
 end
